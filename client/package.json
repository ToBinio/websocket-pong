{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/socket.io-client": "^3.0.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.15"
  },
  "dependencies": {
    "socket.io-client": "^4.5.3"
  }
}
